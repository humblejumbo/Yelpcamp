<!DOCTYPE html>

<html>

<head>
    <title>
        Yelpcamp
    </title>
    <script src="https://use.fontawesome.com/2b702cd8d4.js"></script>
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u"
        crossorigin="anonymous">
    <!-- <link rel="stylesheet" type="text/css" href="../../app.css"> -->
</head>

<body>
    <nav class="navbar navbar-default">
        <div class="container-fluid">
            <div class="navbar-header">

                <a class="navbar-brand" href="/">Yelpcamp</a>
            </div>
            <ul class="nav navbar-nav">
                <li class="active">
                    <a href='/campgrounds'>Discover</a>
                </li>
            </ul>

            <ul class="nav navbar-nav navbar-right">
                <!-- WHETHER TO SHOW LOGIN&SIGNUP OR LOGOUT   -->
                <% if(!currentUser)  {%>
                    <li>
                        <a href="/login">Login
                            <i class="fa  fa-user"></i>
                        </a>
                    </li>
                    <li>
                        <a href="/register">SignUp
                            <i class="fa fa-user-plus"></i>
                        </a>
                    </li>
                    <% }  else { %>
                        <li>
                            <a href='#'>Signed as
                                <%=currentUser.username %>
                            </a>
                        </li>
                        <li>
                            <a href="/logout">Logout
                                <span class="glyphicon glyphicon-log-out" area-hidden='true'></span>
                            </a>
                        </li>
                        <% } %>
            </ul>
        </div>
    </nav>

    <div class="container">

        <!-- SETTING FLASH MESSAGES -->
        <% if(error && error.length>0 ) {%>
            <div class="alert alert-danger " role="alert">
                <%= error %>
            </div>
            <% } %>
                <% if(success && success.length>0) {%>
                    <div class="alert alert-success " role="alert">
                        <%= success %>
                    </div>
                    <% } %>
    </div>

<div class="container">
    <div class="row">
        <h1 class="text-center">Add a New Comment on <%=camp.name %> </h1>
        <div class="container" style="width:30% ; margin:25px auto">
    
       <form action='/campgrounds/<%=camp._id%>/comments' method='post' >
         
        <div class="form-group">
            <textarea class="form-control" name="comment[txt]" placeholder="comment" rows="2"></textarea>
        </div>
      
        <div class="form-group">
             <button class="btn btn-primary btn-lg btn-block text-center">Add</button>
        </div>
   
        </form>

         <a href='/campgrounds/<%=camp._id%>' >GO BACK</a>
 
        </div>
    </div>

</div>


    </body>

</html>